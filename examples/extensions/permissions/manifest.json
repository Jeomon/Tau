{
  "tau": {
    "name": "Permissions",
    "author": "jeomon",
    "dependencies": [
      "bashlex>=0.18"
    ],
    "settings": {
      "title": "Permissions",
      "fields": [
        {
          "key": "headlessDefault",
          "label": "Headless default",
          "type": "string",
          "default": "deny",
          "description": "Decision used when no UI can be asked (print/JSON mode): allow, ask, or deny."
        },
        {
          "key": "promptTimeoutSeconds",
          "label": "Prompt timeout (seconds)",
          "type": "int",
          "default": 600,
          "description": "Unanswered prompts expire to denied. 0 waits indefinitely.",
          "min": 0,
          "max": 86400
        },
        {
          "key": "logDecisions",
          "label": "Log decisions",
          "type": "bool",
          "default": true,
          "description": "Append every decision to .tau/extensions/permissions/decisions.log."
        },
        {
          "key": "hideDeniedTools",
          "label": "Hide denied tools",
          "type": "bool",
          "default": true,
          "description": "Remove fully-denied tools from the schema so the model never probes for them."
        }
      ]
    }
  }
}
