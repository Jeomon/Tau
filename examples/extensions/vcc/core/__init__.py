"""Algorithmic conversation compaction pipeline for tau (ported from vcc).

No LLM calls — a summary is produced purely by extraction and formatting.
The public entry point is ``core.summarize.compile_summary``.
"""
